# common

Shared backend Python code lives under `common/python`.

Use this area for reusable helpers, service clients, configuration utilities, and domain logic that should be shared across FastAPI services.
